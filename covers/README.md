# covers/

封面大图目录：`covers/<slug>.png`，建议 16:9、≤512KB。

- 风格参考：纯色 / 渐变 / 大图均可，客户端会全图铺满渲染
- 文件名与卡片 `background.cover` 字段一致
- 当前样例卡片 `03-open-source-music.json` 引用了 `open-source-music.png`（当前为 1×1 占位 PNG，发布正式内容前请替换）

建议用 AI 绘图或设计工具生成后再提交。